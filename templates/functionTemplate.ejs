'use strict'

module.exports.<%= handlerFunction %> = (event, context, <%= handlerFunction %>Callback) => {
  const response = {
    statusCode: 200,
    body: JSON.stringify({
      message: 'Success!'
    })
  }

  <%= handlerFunction %>Callback(null, response)
}
